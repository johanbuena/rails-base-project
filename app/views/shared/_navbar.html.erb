    <!-- Navbar -->
    <nav class="navbar navbar-expand-lg bg-dark navbar-dark py-3 fixed-top">
      <div class="container">
        <a href="<%= root_path %>" class="navbar-brand">Stocks App</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navmenu">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navmenu">
          <ul class="navbar-nav ms-auto">
            <% if user_signed_in? %>
              <li class="nav-item">
                <a href="<%= edit_user_registration_path %>" class="btn btn-primary m-1">My Account</a>
              </li>
              <li class="nav-item">
                <a href="<%= destroy_user_session_path %>" data-method="delete" class="btn btn-primary m-1">Sign Out</a>
              </li>
            <% else %>
              <li class="nav-item">
                <a href="<%= new_user_session_path %>" class="btn btn-primary m-1">Sign In</a>
              </li>
              <li class="nav-item">
                <a href="<%= new_user_registration_path %>" class="btn btn-primary m-1">Sign Up</a>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>